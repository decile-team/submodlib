#include"SparseVector.h"

SparseVector getSparseVector(std::vector<float> &features) {
    SparseVector s;
    s.indices = std::vector<int>();
    s.values = std::vector<float>();
    for (int i = 0; i < features.size(); i++) {
        if (features[j] != 0) {
            s.indices.push_back(i);
            s.values.push_back(features[i]);
        }
    }
    return s;
}